import { Box } from "@material-ui/core";
import Menu from "../components/menu/Menu.component";

const Home: React.FC = () => {
  return (
    <Box>
      <Menu />
    </Box>
  );
};

export default Home;
