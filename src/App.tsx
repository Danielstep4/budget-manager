import Layout from "./style/Layout";
import Register from "./components/authentication/Register.component";
const App: React.FC = () => {
  return (
    <Layout>
      <Register />
    </Layout>
  );
};

export default App;
